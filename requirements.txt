# FastAPI & Web Framework
fastapi==0.104.1
uvicorn==0.24.0
pydantic>=2.12.0
pydantic-settings>=2.1.0

# Orchestration
# langchain and langchain-openai were declared here but imported nowhere. They
# were also the reason LangGraph could not move: langchain 0.1.1 requires
# langchain-core<0.2, which silently caps langgraph at 0.0.24 with no error.
# Removed, so the generation workflow can use a current LangGraph.
langgraph>=1.2,<2
# Checkpointer: a run that dies after the member summary resumes at the node it
# died on instead of re-generating (and re-paying for) what already succeeded.
# Brings psycopg 3, which coexists with the psycopg2 the app itself uses.
langgraph-checkpoint-postgres>=3.1
# Called directly rather than through a framework wrapper: the LLM writes wording
# only, and the calls are few and explicit.
openai>=1.6.1,<2

# Vector Databases
weaviate-client==4.23.1

# Database & ORM
sqlalchemy==2.0.23
psycopg2-binary==2.9.9

# Environment & Config
python-dotenv==1.0.0
pyyaml==6.0.1

# API & HTTP
httpx>=0.26.0
requests==2.31.0

# Logging & Monitoring
python-json-logger==2.0.7

# Testing
pytest==7.4.3
pytest-asyncio==0.21.1
pytest-cov==4.1.0

# Code Quality
black==23.12.0
flake8==6.1.0
isort==5.13.2

# Security
cryptography==41.0.7

# Async
aiohttp==3.9.1

# Development
ipython==8.17.2
alembic>=1.20.0
